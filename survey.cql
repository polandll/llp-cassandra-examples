use test;
create table survey (submit_time text,location text,start_date text, db_exp int, cass_exp int, had_exp int, solr_exp int, spark_exp int, role text, nps int, instructor text, knowledge int, prepared int, added_value int, personal_interact int, met_needs int, encouraging int, personal_presence int, class_mgmt int, instructor_comment text, uptodate int, sequenced int, exercises int, demos int, enough_handson int, materials_comment text, valuable text, not_valuable text, added_comments text, source text, use_feedback text, name text, email text, phone text, primary key (submit_time,location));
copy survey (submit_time, location, start_date, db_exp, cass_exp, had_exp, solr_exp,spark_exp,role,nps,instructor,knowledge,prepared,added_value,personal_interact,met_needs,encouraging,personal_presence,class_mgmt,instructor_comment,uptodate,sequenced,exercises,demos,enough_handson,materials_comment,valuable,not_valuable,added_comments,source,use_feedback,name,email,phone) FROM 'survey_data.csv' WITH HEADER=true AND delimiter='|';
